<h1>Application Submitted</h1>

<p style="margin: 8px 0 16px;">
    Thanks for applying! Here's your Application
</p>

<div style="border: 1px solid #e5e5e5; border-radius: 10px; padding: 14px; max-width: 560px;">
    <div style="display:flex; gap: 12px; margin-bottom: 8px;">
        <strong style="min-width: 120px;">Name</strong>
        <span>{{applicant.name}}</span>
    </div>

    <div style="display:flex; gap: 12px; margin-bottom: 8px;">
        <strong style="min-width: 120px;">Email</strong>
        <span>{{applicant.email}}</span>
    </div>

    <div style="display:flex; gap: 12px; margin-bottom: 8px;">
        <strong style="min-width: 120px;">Phone</strong>
        <span>{{applicant.phone}}</span>
    </div>

    <div style="display:flex; gap: 12px; margin-bottom: 8px;">
        <strong style="min-width: 120px;">Position</strong>
        <span>{{applicant.position}}</span>
    </div>

    {{#if applicant.resumePath}}
        <div style="display: flex; gap:12px; margin-top:12px;">
            <strong style="min-width: 120px;">Position</strong>
            <span title="{{applicant.resumePath}}">Saved at: <code>{{applicant.resumePath}}</code></span>
        </div>
    {{/if}}
</div>

<form method = "POST" action="/signup/complete" style="margin-top: 16px;">
    <input type="hidden" name="pdf" value="{{applicantId}}">
    <button type="submit" class="btn btn-primary">Done</button>
</form>
<style>
  .btn {
    display:inline-block; padding:8px 12px; border:1px solid #ccc;
    border-radius:8px; text-decoration:none; margin-right:8px;
  }
  .btn:hover { background:#f5f5f5; }
  .btn-primary { border-color:#0d6efd; background:#0d6efd; color:#fff; }
  .btn-primary:hover { filter:brightness(1.05); }
</style>



